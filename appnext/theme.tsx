import { extendTheme } from "@chakra-ui/react";

const mytheme = extendTheme({
  colors: {
    deep2dark: "#1E1E1E",
    deepdark: "#2C2C30",
    dark: "#444449",
    shallowdark: "#6B6B74",
    shallow2dark: "#CCCCCF",
  },
});

export default mytheme;
